version: 1.0

runtime: python311

build:
  commands:
    build:
      - python -m pip install -r requirements.txt
      - npm install

run:
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000